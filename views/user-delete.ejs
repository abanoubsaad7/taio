<!DOCTYPE html>
<html lang="en">

<%- include("partials/head") %>
<body>
    <%- include("partials/header-admin") %>
    <section id="description">
        <div class="container">
            <div>
                <h3 class="type"> the name is <%= objUser.username %> </h3>
                <h6 class="dis">
                   the number is <%= objUser.Number %>
                </h6>
                <h5 class="price"> the password is <%= objUser.password %></h5>
                <button data-linkid="<%= objUser._id %>" class="btn btn-lg btn-danger delete">Delete</button>
            </div>
        </div>
    </section>
    <script>
        const btnelement = document.querySelector(".delete")
        const userId = btnelement.getAttribute("data-linkid");
        btnelement.addEventListener("click", (del) => {
            //data is variable we can store the data from eldata elly inside el json fy el app.js
            fetch(`/user/${userId}`, { method: "DELETE" })
                .then((response) => response.json())
                .then((data) => window.location.href = data.myLink)
                .catch((err) => {
                    console.log(err);
                });

        })
    </script>
    <section id="about">
        <center>
            <img src="/img/logo.png" alt="logo" class="logo">
            <h2>
                   اسرة مارجرجس اعدادي
            </h2>
            <h3>
             اسرة بتحبك ومستنياااااااااااااااااااك
            </h3>
        </center>
    </section>
    <footer>
        <center>
            <h2> متنساش </h2>
            <hr />
            <h2>اسرة بتحبك ومستنيااااك</h2>
          </center>
    </footer>
</body>

</html>