<!DOCTYPE html>
<html lang="en">

<%- include("partials/head") %>
<body>
    <%- include("partials/header-admin") %>
    <section id="description">
        <div class="row">
            <div class="col-lg-5">
                <img src="/productImg/<%= objProduct.imgPath %>" alt="product" class="dis">
            </div>
            <div class="col-lg-5">
                <h3 class="type"><%= objProduct.proTitle %> </h3>
                <h6 class="dis">
                    <%= objProduct.proDescription %>
                </h6>
                <h5 class="price"> <%= objProduct.proPrice %> Taio</h5>
                <button data-linkid="<%= objProduct._id %>" class="btn btn-lg btn-danger delete">Delete</button>
            </div>
        </div>
    </section>
    <script>
        const btnelement = document.querySelector(".delete")
        const proId = btnelement.getAttribute("data-linkid");
        btnelement.addEventListener("click", (del) => {
            //data is variable we can store the data from eldata elly inside el json fy el app.js
            fetch(`/product/${proId}`, { method: "DELETE" })
                .then((response) => response.json())
                .then((data) => window.location.href = data.myLink)
                .catch((err) => {
                    console.log(err);
                });

        })
    </script>
        <section id="about">
            <center>
                <img src="/img/logo.png" alt="logo" class="logo">
                <h2>
                       اسرة مارجرجس اعدادي
                </h2>
                <h3>
                 اسرة بتحبك ومستنياااااااااااااااااااك
                </h3>
            </center>
        </section>
    <footer>
        <center>
            <h2> متنساش </h2>
            <hr />
            <h2>اسرة بتحبك ومستنيااااك</h2>
          </center>
    </footer>
</body>

</html>